name: Github Pipeline Upload
description: Fast pipeline upload without cloning the repo
author: https://github.com/envato/github-pipeline-upload-buildkite-plugin
requirements:
  - curl
configuration:
  properties:
    file:
      type: [string, array]
      minimum: 1
  additionalProperties: false
